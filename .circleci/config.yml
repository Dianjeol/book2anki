version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.8
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            python -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt
            # Download NLTK resources
            python -c "import nltk; nltk.download('punkt'); nltk.download('averaged_perceptron_tagger'); nltk.download('wordnet'); nltk.download('omw-1.4')"
      # Add steps for any tests when they are available
      # - run:
      #     name: Run tests
      #     command: |
      #       . venv/bin/activate
      #       # Replace 'tests' with the path to your test files
      #       python -m unittest discover tests
      # - store_test_results:
      #     path: test-results
      #     # Ensure your tests produce results in the 'test-results' directory

workflows:
  version: 2
  build:
    jobs:
      - build

